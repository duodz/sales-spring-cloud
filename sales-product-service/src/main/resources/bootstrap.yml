spring:
  cloud:
    config:
      profile: dev
      label: dev
      name: sales-product-service
      uri: http://localhost:8099

management:
  endpoints:
    web:
      exposure:
        include: ["refresh"]
